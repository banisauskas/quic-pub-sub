### Run Server

One instance:

```shell
cd server
go run .
```

### Run Publisher Client

Any number of instances:

```shell
cd client-publish
go run .
```

### Run Subscriber Client

Any number of instances:

```shell
cd client-subscribe
go run .
```